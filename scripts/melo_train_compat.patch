diff --git a/melo/download_utils.py b/melo/download_utils.py
index 373244d..787f608 100644
--- a/melo/download_utils.py
+++ b/melo/download_utils.py
@@ -64,4 +64,5 @@ def load_or_download_model(locale, device, use_hf=True, ckpt_path=None):
     return torch.load(ckpt_path, map_location=device)
 
 def load_pretrain_model():
-    return [cached_path(url) for url in PRETRAINED_MODELS.values()]
+    # dead S3 defaults; fine-tune supplies --pretrain_G explicitly, D/dur start fresh
+    return ["", "", ""]
diff --git a/melo/utils.py b/melo/utils.py
index bafca5a..6141d74 100644
--- a/melo/utils.py
+++ b/melo/utils.py
@@ -183,8 +183,8 @@ def plot_spectrogram_to_numpy(spectrogram):
     plt.tight_layout()
 
     fig.canvas.draw()
-    data = np.fromstring(fig.canvas.tostring_rgb(), dtype=np.uint8, sep="")
-    data = data.reshape(fig.canvas.get_width_height()[::-1] + (3,))
+    data = np.frombuffer(fig.canvas.buffer_rgba(), dtype=np.uint8)
+    data = data.reshape(fig.canvas.get_width_height()[::-1] + (4,))[..., :3]
     plt.close()
     return data
 
@@ -214,8 +214,8 @@ def plot_alignment_to_numpy(alignment, info=None):
     plt.tight_layout()
 
     fig.canvas.draw()
-    data = np.fromstring(fig.canvas.tostring_rgb(), dtype=np.uint8, sep="")
-    data = data.reshape(fig.canvas.get_width_height()[::-1] + (3,))
+    data = np.frombuffer(fig.canvas.buffer_rgba(), dtype=np.uint8)
+    data = data.reshape(fig.canvas.get_width_height()[::-1] + (4,))[..., :3]
     plt.close()
     return data
 
