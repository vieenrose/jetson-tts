# Python env (rebuild via docs/ENV_SETUP.md)
.venv/
__pycache__/
*.pyc

# Generated training data (17h teacher dump — reproducible via scripts/dump_teacher.py)
data/pairs/
data/eval_pairs/
data/_*/
data/zh_config.json

# Model artifacts (large; reproducible)
*.onnx
*.pt
*.pth
student/runs/
export/*/
eval/wavs/
eval/*.wav
eval/_*.wav

# Logs / scratch
logs/
*.log
nohup.out

# Third-party source clones (cloned + patched via scripts/melo_zh_en_lazyimport.patch)
third_party/MeloTTS/

# OS
.DS_Store

# Matcha 8k working data (models, mels, eval wavs — local/HF only)
matcha_eval/

# large model downloads + qwen corpus (local)
models/
.venv-qwen/

# training checkpoints (large, reproducible)
matcha8k/ft_runs/
matcha8k/runs/
student/runs/
# cloned third-party source (clone per docs/ENV_SETUP; matcha8k plan)
third_party/BreezyVoice/
third_party/Matcha-TTS/
